import 'package:flutter/material.dart';

class NewButtom extends StatelessWidget {
  const NewButtom({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: Text("this is button"),
    );
  }
}
